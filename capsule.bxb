capsule {
  id (viv.stackoverflow)
  version (0.1.0)
  format (3)
  targets {
    target (bixby-mobile-en-US)
  }
  capsule-imports {
    import (viv.entity) {as (entity) version(3.10.0)}
    import (viv.geo) {as (geo) version (9.3.0)}
    import (viv.time) {as (time) version (2.62.2)}
  }
}
